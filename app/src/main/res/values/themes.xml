<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.RevancedManager" parent="android:Theme.Material.Light.NoActionBar" />
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color to black -->
        <item name="windowSplashScreenBackground">@color/black</item>

        <!-- Use the app icon as the splash image -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        <!-- Set the theme of the Activity that directly follows your splash screen -->
        <item name="postSplashScreenTheme">@style/Theme.RevancedManager</item>
    </style>
</resources>